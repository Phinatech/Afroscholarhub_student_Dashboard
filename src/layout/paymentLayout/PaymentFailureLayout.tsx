// import React from 'react'

import { Outlet } from "react-router-dom"

const PaymentFailureLayout = () => {
  return (
    <div>
        <Outlet />
    </div>
  )
}

export default PaymentFailureLayout